# Todo: Fix Floating Action Dock & Rename to Resume

- [x] Task 1: Update `frontend/app/globals.css` route entrance animation to pure opacity fade (preventing `position: fixed` containment)
- [x] Task 2: Rename "Executive CV (PDF)" to "Resume" in `FloatingActionDock.tsx` and place dock outside `<main>` in `frontend/app/page.tsx`
- [ ] Task 3: Build Next.js frontend, restart `alexseif-frontend.service`, and verify fixed floating dock on `http://localhost:3000/`
- [ ] Task 4: Push all commits to `origin/master` per user request ("great ready to push")
