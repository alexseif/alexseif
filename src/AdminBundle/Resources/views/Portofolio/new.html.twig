{% extends 'AdminBundle:Default:base.html.twig' %}

{% block container %}
  <h1>Portofolio creation</h1>
  <a href="{{ path('portofolio_index') }}">Back to the list</a>

  {{ form_start(form) }}
  {{ form_widget(form) }}
  <input type="submit" value="Create" />
  {{ form_end(form) }}

{% endblock %}
