<header class="nav">
  <div class="container">
    <div id="nav">
      <div class="brand flow align-items-center">
        <a href="{{ path('alx_home') }}"><img src="{{ asset('build/images/Wordpress-Optimize-logo.png') }}" alt="WordPress Optimization, speed up Wordpress" width="60" /></a>
        <h1>{{ 'wordpress_optimize.header.title'|trans }}</h1>
      </div>
      <!-- .brand -->
      <nav role="navigation">
        <ul>
          <li>
            <a href="#personal-intro">👋🏾 Hi</a>
          </li>
          <li>
            <a href="#services" title="{{ 'wordpress_optimize.navigation.what'|trans }}">{{ 'wordpress_optimize.navigation.what'|trans }}</a>
          </li>
          <li>
            <a href="#clients" title="{{ 'wordpress_optimize.navigation.who'|trans }}">{{ 'wordpress_optimize.navigation.who'|trans }}</a>
          </li>
          <li>
            <a href="#why" title="{{ 'wordpress_optimize.navigation.why'|trans }}">{{ 'wordpress_optimize.navigation.why'|trans }}</a>
          </li>
          <li>
            <a href="#contact" title="{{ 'wordpress_optimize.navigation.where'|trans }}">{{ 'wordpress_optimize.navigation.where'|trans }}</a>
          </li>
          <li>
            <a href="https://wa.me/+201004006332" target="_blank" class="accent">Yalla</a>
          </li>
        </ul>
      </nav>
    </div>
    <!-- #nav -->
  </div>
</header>

<!-- Navigation Menu -->
<nav class="nav-menu" id="nav">
  <div class="container">
    <nav id="navbarNav">
      <ul class="navbar-nav flex-column">
        <li>
          {% if app.request.locale != 'en' %}
            <a title="English" href="{{ path('app_landing_page_locale', { '_locale': 'en' }) }}">EN</a>
          {% endif %}
          {# {% if app.request.locale != 'ar' %} #}
          {# <a title="عربي" href="{{ path("app_landing_page_locale", {"_locale":"ar"}) }}">ع</a> #}
          {# {% endif %} #}
          {% if app.request.locale != 'eg' %}
            <a title="مصري" href="{{ path('app_landing_page_locale', { '_locale': 'eg' }) }}">🇪🇬</a>
          {% endif %}
        </li>
      </ul>
    </nav>
  </div>
</nav>
